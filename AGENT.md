# AGENT.md — 블로그 리뷰 진행 가이드 (에이전트용 단일 진실)

이 파일은 **에이전트(Claude Code/Codex)가 리뷰를 진행할 때 따르는 규칙**이다.
사람용 로그는 `README.md`에 있다. 둘을 섞지 말 것.

## 프로젝트 목적
기업 테크 블로그를 하루 1개 리뷰한다. 직접 겪기 힘든 엔지니어링 상황을 **대리 경험**하고,
유사 상황에서 **빠르게 사고·접근**하도록 사용자의 **사고 자산**으로 만드는 것.
→ 그래서 목표는 "글 요약"이 아니라 **사용자의 사고를 끌어내고 교정하는 것**이다.

## 진행 컨텍스트 (결정 이력 — 이 레포가 유일한 기억)
- **주중(월~금)만 진행.** 주말은 쉼 → 연속일수 트래킹 안 함.
- **노트 깊이는 첫 리뷰(nextree 웹소켓) 수준을 지향** (사용자가 "딱 좋다" 확정). 더 얕게/무겁게 하지 말 것.
- 노트 형식은 "대화형 학습 루프"(내가 이해한 것→교정)가 이 사용자에게 맞음. 무미건조한 문제/해결 템플릿 지양.
- **2026-08-10 개편:** 노트가 기준의 6배까지 부풀어(329줄) 회고가 불가능해짐. 원인의 절반이 **범용 개념 설명**이었고, 버리는 대신 `concepts/`로 **이관**하기로 결정. 같이 정한 것 — 섹션을 필수 5 / 옵션 4로 가르고, 정기 회고 규칙은 지우고 상시 연결(개념 재방문 + 노트 상호링크)로 대체.
- **2026-08-11 결정:** 항목 수가 다시 불었을 때(교정 8 / 패턴 15) **상한은 두지 않기로** 했다. 대화가 좋아서 늘어난 걸 개수로 자르면 내용이 날아간다. 대신 **병합**한다 — 노트를 닫기 전에 `🔁 패턴`을 훑고 **같은 얘기의 다른 각도인 항목들을 한 항목으로 합친다.** 하위 불릿으로 각도를 남기면 내용은 안 잃고 인출 단위만 줄어든다. (그날 15→9)
  - 병합 기준: *"이 둘이 무너지는 조건이 같은가?"* 같으면 한 항목. 예 — "종속이 의존성 목록 밖에 숨는다"와 "티 안 나는 결함이 오래 산다"는 **둘 다 '감사되지 않으면 관리되지 않는다'**라서 합쳤다.

## 3원칙
1. **가볍게 (15~20분).** 습관이 1순위. 못 채운 칸 있어도 넘어간다. 완벽함보다 연속성.
2. **요약을 대신 해주지 말 것.** 문제·해결 정리는 짧게. 사용자가 "내가 이해한 것 / 생각 / 질문"을 내놓게 하고, 거기에 반응한다.
3. **교정을 남긴다.** 확인된 것보다 교정당한 것이 오래 남는다. 노트의 중심은 "내가 이렇게 알았는데 → 진짜는 이거".

## 일일 흐름 (5단계)
1. **링크 접수** — 사용자가 URL만 준다.
2. **뼈대만 깐다** — WebFetch로 메타데이터 + 중립 요약(내 해석 배제) + 글의 뼈대(중심 주장 1개, 이득, 미해결)만. 여기서 평가·의견 넣지 말 것.
3. **사용자가 날것 투척** — 이해한 흐름 / 반응 / **모르겠는 것·질문**. (이 단계가 제일 값지다. 막히면 질문 1개만 나와도 성공.)
4. **교정 대화** — 각 항목에 `✅ 맞음` / `🔧 교정` / `💡 한 단계 더` 표기로 답한다. 꼬리물기 환영. 정확성 최우선(틀린 걸 확인해주지 말 것).
5. **노트로 묶고 커밋한다** — 아래 형식으로 `reviews/`에 저장 → **이 단계에서만 raw 사용자 입력을 일괄 저장** → **개념은 `concepts/`로 분리** → (해당되면 `TRY.md`/`QUESTIONS.md` 갱신) → `README.md` 로그/현황 갱신 → **자동 커밋**(아래 규칙).
   - README 로그 행 컬럼: `# | 날짜 | 회사 | 글 (원문) | 한 줄 | 태그 | 노트`
   - "글 (원문)"은 **글 제목에 원문 URL 링크**를 건다: `[글 제목](원문링크)`. "노트"는 `reviews/` 파일 링크.

### raw 사용자 입력은 마지막에 한 번만 저장한다

raw는 대화 백업이 아니라 **교정 전 사고의 원문 데이터**다. 질문에 답하는 중에는 파일을 쓰거나 스크립트를 실행하지 말고, 5단계에서 노트를 저장할 때 한 번에 처리한다.

- **수집 경계:** 블로그 URL을 받은 시점부터 해당 리뷰를 처음 저장해 푸시하는 시점까지.
- **포함:** 사용자가 직접 보낸 URL, 이해, 생각, 질문, **사고 내용에 영향을 주는 요청**. 복수 성격이면 `kinds`에 여러 값을 넣는다.
- **제외:** 에이전트 답변, 도구 출력, 원문 글 본문, 최종 노트에서 새로 생성한 문장, **저장·정리·커밋 등 리뷰 진행만 지시하는 행정적 요청**.
- **원문 불변:** `text`의 맞춤법·띄어쓰기·줄바꿈·표현을 요약하거나 고치지 않는다.
- **예외:** 자격증명·토큰·비밀키와 사용자가 비공개로 지정한 문구만 `[REDACTED: 유형]`으로 치환한다. 이때 `redacted: true`와 `redaction_types`를 남긴다.
- **소급 금지:** 기존 리뷰는 원문을 재구성하지 않는다. raw는 이 규칙을 도입한 뒤의 새 리뷰부터 생성한다.

저장 순서:
1. 리뷰 frontmatter에 `raw: ../raw/<review-id>.jsonl`을 넣고 리뷰 파일을 먼저 만든다.
2. 대화에서 사용자 입력만 순서대로 모아 `/tmp/blog-review-raw-<review-id>.json`에 아래 manifest로 쓴다.
3. `python3 .claude/skills/blog-review/scripts/write_raw.py --review reviews/<review-id>.md --input /tmp/blog-review-raw-<review-id>.json --output raw/<review-id>.jsonl`을 실행한다.
4. 스크립트가 비밀정보 후보를 발견하면 원문을 제거하지 말고 manifest에서 해당 값만 `[REDACTED: credential]`로 바꾸고 메타데이터를 설정한 뒤 재실행한다.
5. 성공·실패와 관계없이 임시 manifest를 지우고, 생성된 JSONL과 리뷰를 같은 커밋에 포함한다.

manifest 형식:
```json
{
  "review_id": "2026-08-14-company-topic",
  "entries": [
    {
      "kinds": ["understanding", "question"],
      "text": "사용자가 실제로 입력한 원문",
      "redacted": false
    }
  ]
}
```

허용 `kinds`: `source` `understanding` `thought` `question` `request`. 스크립트가 `schema_version: 1`과 1부터 시작하는 `sequence`를 부여한다. 세부 스키마와 분석 예시는 `raw/README.md`를 따른다.

### 개념은 노트에 쓰지 않는다 — `concepts/`로 뺀다
범용 용어·기술 개념(웹뷰가 뭔가, breaking change 판정 기준, Zod가 뭔가)은 **글이 없어도 재사용되고 다음 글에서 또 나온다.** 노트에 흩어놓으면 인출이 안 되고 설명이 중복된다.

- **가르는 선:** *그 글의 논지를 이루는가?* → 노트에 남긴다. *글이 없어도 성립하는 정의인가?* → `concepts/`로 뺀다.
- 노트에는 **링크만** 건다: `→ 🔧 새 개념 아님 ([안드로이드 백키](../concepts/android-back-key.md)).`
  개념 설명을 노트에 다시 적지 말 것. 대신 **"이 글에서 그게 왜 중요한가"**를 한 줄 남긴다.
- 개념 하나 = 파일 하나. 파일명은 **영문 케밥 슬러그**(`android-back-key.md`), 제목(`# `)은 한글/원어.
- 각 개념 파일 끝에 **관련 개념 링크**와 **처음 나온 노트 링크**를 남기고, `concepts/README.md` 목록에도 한 줄 추가한다.

### 노트끼리 링크한다
이전 노트와 **같은 모양의 패턴**이 나오면 그 자리에서 링크한다 — `([AI 자산 노화](2026-08-07-yeogieottae-ai-asset-aging.md)의 "트리거를 밖에서 빌려라"와 같은 모양)`.
회고 이벤트 없이도 연결이 상시 쌓이게 하는 장치다.

### 중앙 파일 두 개 (둘 다 **사용자가 남긴 것만**, 항목마다 출처 노트 링크 필수)
- **`QUESTIONS.md`** — 그날 못 닫았거나 더 파고 싶은 **질문**. 대개 대화로 해소되니 보통 비어 있다. 안 닫힌 것만 남긴다.
- **`TRY.md`** — 글 개념으로 **직접 해볼 실습 아이디어**. 옵트인 — 사용자가 실습거리를 원하거나 좋은 아이디어가 나왔을 때만.
- ⚠️ **에이전트가 멋대로 채우지 않는다.** "해볼 만하겠다" 싶은 건 그 순간 대화(💡)로만 던지고, **사용자가 넣겠다고 한 것만** 파일에 기록한다.
- (`concepts/`는 예외 — 대화에서 실제로 설명한 개념은 에이전트가 정리해 넣는다.)
- **코드로 실습할 땐 `examples/<주제-슬러그>/`에 프로젝트를 만든다.** 폴더·파일명에는 회사명을 넣지 않고 기술·개념·문제만 적는다(예: `examples/web-components/`, `examples/rxjs-side-effects/`). 출처 회사·글은 프로젝트 최상위 `README.md` 맨 위에 **어느 리뷰에서 왔는지 링크**(`reviews/...md`)로만 남긴다. 하위 실습이 여러 개면 폴더별로 쪼개고, 각 폴더 README에도 같은 출처 링크를 남긴다. 가능하면 의존성 0개(언어 내장 기능만)로 — `npm install` 없이 바로 실행 가능해야 습관이 안 끊긴다.

> 시간 없는 날: 3단계까지(링크 + 생각 한 줄 + 질문 한 개)만 남겨도 그날은 성공.

## 자동 커밋 + 푸시 (저장할 때마다 필수)
리뷰를 저장하거나 이 레포 파일을 바꾸면, 그 흐름의 **마지막에 반드시 커밋하고 origin에 푸시**한다. 물어보지 말고 자동으로.
```bash
git add -A && git -c user.name="zaehorang" -c user.email="jaho000101@gmail.com" \
  commit -q -m "리뷰: <회사> <키워드> (YYYY-MM-DD)" && git push -q
```
- 리뷰 저장이면 메시지는 `리뷰: <회사> <키워드> (날짜)`. 규칙/구조 변경이면 무슨 변경인지로.
- Co-Authored-By 트레일러는 넣지 않는다 (사용자 선호).
- 푸시가 실패하면(네트워크/충돌 등) 사용자에게 알린다. 커밋은 이미 로컬에 남아있으니 나중에 재시도 가능.

## 노트 형식
파일명: `reviews/YYYY-MM-DD-회사-키워드.md` (예: `2026-07-30-nextree-websocket-chat.md`)

프론트매터:
```yaml
---
date: YYYY-MM-DD
company: 회사 (작성자)
source: 원문 링크
tags: [도메인, 문제패턴]   # 아래 어휘에서 2~4개
raw: ../raw/YYYY-MM-DD-회사-키워드.jsonl  # raw 규칙 도입 후 새 리뷰만
---
```

**필수 섹션 (매번 — 이게 노트의 뼈대다):**
- `# 제목`
- `## 🔑 한 줄 요약` — 나중에 이 줄만 훑어도 떠오르게
- `## 📌 무슨 글` — 중립·짧게 (3줄 내외)
- `## 🧠 내가 이해한 것 → 교정` — **핵심 섹션.** 내 생각 → ✅/🔧/💡
- `## 🔁 도메인 안 가리는 패턴` — 전이 가능한 일반 규칙 (사고 자산의 핵심)
- `## 🎯 오늘 챙길 한 줄` — 규칙/휴리스틱/질문 형태

**옵션 섹션 (그날 진짜 있을 때만 — 칸을 채우려고 만들지 말 것):**
- `## ❓ 궁금증 → 해소` — **글 맥락 질문만.** 범용 개념 질문은 `concepts/`로 (위 규칙)
- `## ⚖️ 비교표` — 대안 설계가 있을 때만
- `## 🐛 겪은 문제 & 해결` — 글에 사건이 있으면
- `## 🛠️ 직접 만들어본 것` — `examples/`에 실습을 만들었으면

> 노트에는 `🔮 미해결/다음에 팔 것` 섹션을 두지 않는다. 안 닫힌 질문·실습거리는 아래 중앙 파일로 뺀다.

## 태그 어휘 (일관성 유지 — 없으면 추가하고 이 목록 갱신)
**도메인:** `backend` `frontend` `mobile` `ios` `android` `infra` `devops` `data` `ml` `platform`
**문제 패턴:** `성능` `장애대응` `확장성` `마이그레이션` `비용최적화` `관측성` `데이터정합성` `동시성` `아키텍처` `리팩터링` `조직/프로세스` `테스트` `보안` `실시간` `타입시스템`

## 검색 / 인출
- 태그·키워드로 한 번에: `grep -rl "장애대응" reviews/`
- 개념은 `concepts/README.md` 목록에서. 같은 개념이 다시 나오면 **새로 설명하지 말고 그 파일을 갱신**한다.
- 별도의 정기 회고 이벤트는 두지 않는다. 대신 **상시 연결**로 대체한다 — 개념은 `concepts/`에서 재방문되고, 같은 모양의 패턴은 노트끼리 링크로 이어진다.
