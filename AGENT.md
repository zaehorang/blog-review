# AGENT.md — 이 레포의 리뷰 규칙

이 파일은 **에이전트가 이 레포에서 리뷰를 진행할 때 따르는 규칙**이다.
사람용 로그는 `README.md`에 있다. 둘을 섞지 말 것.

## 방법은 `blog-review` 스킬에 있다

리뷰 진행 방법(5단계 흐름, 3원칙, ✅/🔧/💡 교정 표기, 노트 형식, 태그 어휘, `concepts/` 분리, raw 캡처)은
**글로벌 스킬 `blog-review`** 가 담는다 → [zaehorang/agent-skills](https://github.com/zaehorang/agent-skills/tree/main/blog-review)

**이 파일은 그 방법을 다시 적지 않는다.** 여기엔 이 레포에만 해당하는 것 — 결정 이력, 경로, 커밋 규칙 — 만 남긴다.
충돌하면 **이 파일이 우선**한다.

## 프로젝트 목적
기업 테크 블로그를 하루 1개 리뷰한다. 직접 겪기 힘든 엔지니어링 상황을 **대리 경험**하고,
유사 상황에서 **빠르게 사고·접근**하도록 사용자의 **사고 자산**으로 만드는 것.
→ 그래서 목표는 "글 요약"이 아니라 **사용자의 사고를 끌어내고 교정하는 것**이다.

## 이 레포가 정한 값 (스킬이 물어보는 것들)

| 항목 | 값 |
|---|---|
| 진행 주기 | **주중(월~금)만.** 주말은 쉼 → 연속일수 트래킹 안 함 |
| 소요 시간 목표 | **15~20분.** 습관이 1순위, 완벽함보다 연속성 |
| 노트 깊이 기준 | [`2026-07-30-nextree-websocket-chat.md`](reviews/2026-07-30-nextree-websocket-chat.md) — 사용자가 "딱 좋다" 확정. 더 얕게/무겁게 하지 말 것 |
| 태그 어휘 | 아래 목록에서만 고른다 |

### 태그 어휘 (여기가 유일한 목록 — 없으면 추가하고 이 줄을 갱신한다)

**도메인**
`backend` `frontend` `mobile` `ios` `android` `infra` `devops` `data` `ml` `platform`

**문제 패턴**
`성능` `장애대응` `확장성` `마이그레이션` `비용최적화` `관측성` `데이터정합성` `동시성` `아키텍처` `리팩터링` `조직/프로세스` `테스트` `보안` `실시간` `타입시스템`

## 진행 컨텍스트 (결정 이력 — 이 레포가 유일한 기억)
- 노트 형식은 "대화형 학습 루프"(내가 이해한 것→교정)가 이 사용자에게 맞음. 무미건조한 문제/해결 템플릿 지양.
- **2026-08-10 개편:** 노트가 기준의 6배까지 부풀어(329줄) 회고가 불가능해짐. 원인의 절반이 **범용 개념 설명**이었고, 버리는 대신 `concepts/`로 **이관**하기로 결정. 같이 정한 것 — 섹션을 필수 5 / 옵션 4로 가르고, 정기 회고 규칙은 지우고 상시 연결(개념 재방문 + 노트 상호링크)로 대체.
- **2026-08-11 결정:** 항목 수가 다시 불었을 때(교정 8 / 패턴 15) **상한은 두지 않기로** 했다. 대화가 좋아서 늘어난 걸 개수로 자르면 내용이 날아간다. 대신 **병합**한다.
  - 병합 기준: *"이 둘이 무너지는 조건이 같은가?"* 같으면 한 항목. 예 — "종속이 의존성 목록 밖에 숨는다"와 "티 안 나는 결함이 오래 산다"는 **둘 다 '감사되지 않으면 관리되지 않는다'**라서 합쳤다. (그날 15→9)
- **2026-09-07 결정:** 리뷰 방법을 이 파일에서 `blog-review` 스킬로 뺐다.
  - 가르는 기준: *"다른 사람이 이 스킬을 깔았을 때 이 줄이 그 사람에게도 참인가?"* 참이면 스킬, "내가 그렇게 정했다"면 이 파일.
  - **참이지만 값이 사람마다 다른 것**(태그 어휘, 소요 시간, 노트 깊이 기준)은 스킬엔 *"정해야 한다"*만 두고 **값은 이 파일**에 둔다. 특히 태그 어휘 — 개인 어휘를 늘리려고 공유 스킬을 고치게 되면 분리가 잘못된 것이다.

## 이 레포의 경로

| 무엇 | 어디 |
|---|---|
| 리뷰 노트 | `reviews/YYYY-MM-DD-회사-키워드.md` |
| raw 원문 | `raw/<review-id>.jsonl` — 데이터 형식 설명은 [`raw/README.md`](raw/README.md) |
| 개념 사전 | `concepts/` + `concepts/README.md` 목록 |
| 동작 원리 시각화 | `explainers/<주제-슬러그>/index.html` |
| 손으로 짜보는 실습 | `examples/<주제-슬러그>/` |
| 안 닫힌 질문 · 해볼 것 | `QUESTIONS.md` · `TRY.md` |

- **`README.md` 로그 행:** `# | 날짜 | 회사 | 글 (원문) | 한 줄 | 태그 | 노트`
  "글 (원문)"은 글 제목에 원문 URL 링크(`[글 제목](원문링크)`), "노트"는 `reviews/` 파일 링크. 현황 줄(총 리뷰/개념/실습 수, 최근 리뷰 날짜)도 같이 갱신한다.
- **`explainers/`** — `mechanism-explainer` 스킬의 산출물 위치. 스킬은 위치를 모르니 **이 줄이 그 정의다.** 폴더명엔 회사명 없이 기술·개념만. 파일 안 푸터에 출처 리뷰(`../../reviews/...md`)와 원문을 링크하고, `explainers/README.md` 목록에도 한 줄 추가.
- **`examples/`** — 폴더·파일명에 회사명을 넣지 않고 기술·개념·문제만 적는다(`examples/web-components/`). 출처는 프로젝트 `README.md` 맨 위에 리뷰 링크로만. 가능하면 의존성 0개 — `npm install` 없이 바로 실행돼야 습관이 안 끊긴다.
- `explainers/`와 `examples/`를 구분한다 — 앞은 **읽고 이해하는 자료**, 뒤는 **손으로 만드는 것**.

## 자동 커밋 + 푸시 (저장할 때마다 필수)
리뷰를 저장하거나 이 레포 파일을 바꾸면, 그 흐름의 **마지막에 반드시 커밋하고 origin에 푸시**한다. 물어보지 말고 자동으로.
```bash
git add -A && git commit -q -m "리뷰: <회사> <키워드> (YYYY-MM-DD)" && git push -q
```
- 리뷰 저장이면 메시지는 `리뷰: <회사> <키워드> (날짜)`. 규칙/구조 변경이면 무슨 변경인지로.
- **작성자(user.name/email)를 `-c`로 지정하지 않는다.** git 설정에 이미 있는 값을 쓴다.
  문서에 이메일을 적어두면 설정과 어긋나고, 덮어쓰기 때문에 **틀린 값이 이긴다.**
  (실제로 그랬다 — 문서에 박힌 부계정 이메일이 전역 설정을 덮어써서 커밋이 다른 계정으로 귀속됐다.)
- Co-Authored-By 트레일러는 넣지 않는다 (사용자 선호).
- 푸시가 실패하면(네트워크/충돌 등) 사용자에게 알린다. 커밋은 이미 로컬에 남아있으니 나중에 재시도 가능.
